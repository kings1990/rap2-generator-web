server:
  port: 6666

spring:
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB
  thymeleaf:
    cache: false
    encoding: UTF-8
    mode: HTML
    prefix: classpath:/templates/thymeleaf/
    
logging:
  level: 
    root: info